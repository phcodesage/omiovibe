# Server
PORT=3001
# Comma-separated or single origin allowed by CORS and Socket.IO
CLIENT_ORIGIN=http://localhost:5173

# Client (Vite)
# IMPORTANT: Must start with VITE_ to be exposed to the client at build/runtime
VITE_SERVER_URL=http://localhost:3001
